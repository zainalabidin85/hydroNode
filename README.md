# hydroNode
This code uses three sensors: DS18B20, Submerssible Water Level Sensor and EC sensor. The code publish the data on its web UI. The web UI has an option for user to enable MQTT service for Home Assistant connection. 
